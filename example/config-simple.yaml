# Simple HTTP server configuration
default:
  server:
    host: "0.0.0.0"
    port: 8080
  directory:
    path: "./public"
    allow_symlink: true
    hide_hidden: true
  features:
    compression: ["gzip", "brotli"]
    stats_file: "data/stats-simple.json"
